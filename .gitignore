# Build artifacts (produced by `make` / `makepkg`)
*.o
*.obj
*.so
*.efi
*.a
*.lib
*~

# makepkg
/pkg/
/src/
*.pkg.tar.*
*.log

# makepkg's local VCS source cache (bare git dir it creates alongside PKGBUILD)
/pearos-bootloader/
